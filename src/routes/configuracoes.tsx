import { createFileRoute,useNavigate } from "@tanstack/react-router";import { useState } from "react";import { AppShell } from "@/components/layout/AppShell";import { PageHeader } from "@/components/common/ui-kit";import { clearData } from "@/lib/store";
export const Route=createFileRoute("/configuracoes")({component:Page});function Page(){const nav=useNavigate();const [confirm,setConfirm]=useState(false);return <AppShell><PageHeader title="Configurações" subtitle="Preferências e manutenção dos dados locais deste MVP."/><section className="panel max-w-2xl p-6"><h2 className="font-display font-semibold">Armazenamento local</h2><p className="mt-2 text-sm text-muted-foreground">Nesta versão os dados ficam no navegador deste computador. A migração para Supabase será feita posteriormente.</p><div className="mt-5 border-t border-border pt-5"><h3 className="font-semibold text-destructive">Limpar dados importados</h3><p className="mt-1 text-sm text-muted-foreground">Remove campanhas e histórico deste navegador. Não altera nada no Meta Ads.</p>{!confirm?<button onClick={()=>setConfirm(true)} className="mt-4 rounded-lg border border-destructive/40 px-4 py-2 text-sm text-destructive">Limpar dados…</button>:<div className="mt-4 flex gap-2"><button onClick={()=>{clearData();nav({to:'/visao-geral'})}} className="rounded-lg bg-destructive px-4 py-2 text-sm font-semibold text-white">Confirmar exclusão local</button><button onClick={()=>setConfirm(false)} className="rounded-lg border border-border px-4 py-2 text-sm">Cancelar</button></div>}</div></section></AppShell>}
